export const metadata = {
  title: 'List Belanja',
  description: 'belajar react',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
